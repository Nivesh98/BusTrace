import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const PassengerSupportDrawer = () => {
  return (
    <View>
      <Text>PassengerSupportDrawer</Text>
    </View>
  );
};

export default PassengerSupportDrawer;

const styles = StyleSheet.create({});
